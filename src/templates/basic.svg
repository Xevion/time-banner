<svg xmlns="http://www.w3.org/2000/svg" font-family="Roboto Mono"
    font-size="27" width="{{ width }}" height="{{ height }}" viewBox="0 0 {{ width }} {{ height }}">
    <text x="12" text-anchor="start" dy="24">{{ text }}</text>
</svg>